name: Pull Docker Image

on:
  push:
    branches:
      - main

jobs:
  test-seperated-action:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: test action
        run: echo "test successful"
